# Exécution du Plan 022

Le Plan 022 a été exécuté sur la branche
`codex/022-three-os-rust-gate`, à partir de `86ae89b`. Le drift check demandé a
signalé les écarts attendus des Plans 020 et 021 ainsi que de l’ADR 0014 dans
les workflows et la documentation. Leur comparaison avec les extraits live a
confirmé que le job Linux borné, ses étapes Rust et la politique de workflow
restaient compatibles avec ce plan.

## Réalisation

- Rust `1.91.0`, version active lors de l’exécution, est épinglé dans
  `rust-toolchain.toml` et dans les quatre étapes de toolchain existantes.
- Le workflow `Portability` exécute formatage, Clippy avec avertissements
  interdits et tests Rust sur `macos-15` et `windows-2025`. Le job `Quality`
  conserve le même gate sur `ubuntu-24.04`.
- La politique vérifie désormais les extensions `.yml` et `.yaml`, le contrat
  du workflow `Portability` et l’existence du workflow `Security` planifié.
- Le README, le guide de lancement et les contrats de test, de release et de
  support décrivent les prérequis macOS, Debian/Ubuntu et Windows ainsi que la
  portée exacte du gate automatique.
- Les descriptions périmées des Plans 014 et 015 ont été alignées sur la
  matrice restaurée.

## Vérifications locales

- `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked` :
  réussi.
- `bun scripts/check-workflows.ts` : réussi avec les six workflows.
- Le test négatif sans `portability.yml` échoue avec le message de fichier
  manquant, puis le fichier est restauré.
- Le gate Rust complet — formatage, Clippy et tests — est exécuté localement
  avant le commit.
- `bun run check` et le test Rust final sont exécutés avant le commit.

## Gate distant différé

`AWAITING OPERATOR: first Portability run deferred until final CI pass`

Le run `Quality` 31899062049 n’a pas démarré en raison d’un paiement récent
échoué ou d’une limite de dépenses GitHub Actions à augmenter. L’opérateur a
explicitement choisi de différer les GitHub Actions jusqu’à la passe CI finale.
Aucun run `Portability` n’est donc lancé et aucune preuve macOS/Windows distante
n’est revendiquée. Le plan reste `AWAITING OPERATOR` plutôt que `DONE`.

Le commit unique demandé porte le message
`ci: restore the three-os rust verification gate`.
