# Exécution du Plan 024

Le Plan 024 a été exécuté sur la branche
`codex/024-linux-windows-bundles`, à partir de `c04bfad`. Le drift check exact a
signalé uniquement les changements attendus des Plans 022 et 023 ainsi que la
mise en conformité documentaire avec l’ADR 0014. Les extraits live sont restés
compatibles, Plan 015 n’avait pas modifié `bundle.*`, et aucune condition STOP
n’a été rencontrée.

## Réalisation

- La configuration Tauri sélectionne les bundles natifs macOS, Linux et Windows.
- Les paquets Debian et RPM déclarent leurs dépendances d’exécution ; AppImage
  n’embarque pas le framework média.
- Les installateurs Windows utilisent le bootstrapper WebView2 embarqué et NSIS
  s’installe pour l’utilisateur courant en anglais ou en français.
- Les restes mobiles ont été retirés : configuration Android, types de crate
  mobiles, attribut d’entrée mobile et icônes Android/iOS.
- Le workflow de review conserve `--no-sign` et utilise désormais la
  configuration versionnée pour choisir les bundles.
- Les tests de configuration verrouillent les cibles, les dépendances Linux,
  le mode WebView2 et l’absence de configuration Android.
- La documentation décrit les installateurs unsigned, leur posture de confiance
  et les vérifications physiques Linux/Windows à enregistrer.

Le plan omettait un consommateur direct des icônes supprimées :
`apps/desktop/src/app/theme-contract.test.ts` exigeait encore leur présence.
Après autorisation explicite de l’opérateur, le scope a été étendu uniquement à
ce fichier pour retirer les vingt références Android/iOS et garder le contrat
des assets desktop cohérent.

## Vérifications

- `bunx tauri info` : configuration acceptée sans erreur de schéma.
- Trois builds macOS, dont un après la suppression effective des restes mobiles : `.app`
  et `.dmg` produits avec la signature ad hoc attendue.
- Clippy avec `-D warnings` et les tests Rust : réussis.
- `bun run test:desktop` et `bun scripts/check-workflows.ts` : réussis.
- Les gates finaux `bun run check` et Cargo : réussis.

`AWAITING OPERATOR: Linux/Windows install checks and review-builds run deferred until final pass`

Le workflow GitHub n’a pas été dispatché. Les installations et lancements des
artifacts Linux/Windows n’ont pas été déclarés acquis et restent à vérifier sur
les machines ou VM correspondantes lors de la passe finale.

Le commit unique demandé porte le message
`build(desktop): configure linux and windows bundles`.
