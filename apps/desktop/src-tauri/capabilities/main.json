{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Core main-window permissions plus explicit text clipboard writes; Workspace and capture use custom Rust commands",
  "windows": ["main"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}
