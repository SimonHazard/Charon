{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions only; Workspace and directory selection use custom Rust commands",
  "windows": ["main"],
  "permissions": ["core:default"]
}
