{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions plus explicit text clipboard writes; Workspace and directory selection use custom Rust commands",
  "windows": ["main", "capture"],
  "permissions": ["core:default", "clipboard-manager:allow-write-text"]
}
